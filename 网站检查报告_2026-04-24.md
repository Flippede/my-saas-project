# 网站检查报告

- 检查日期：2026-04-29
- 检查范围：首页、登录页、仪表盘、脚本生成页、生成工作台、支付页、旧版视频工具页，以及前后端关键代码链路
- 检查方式：源码复检、前端生产构建、后端语法/导入检查、FastAPI 本地接口探针
- 当前结论：上一版多数 P0/P1 问题已经修复，但网站仍不建议直接上线；当前最主要阻断变为前端生产构建失败、运行环境依赖不完整，以及少量旧入口/占位链接残留。

## 一、复检摘要

### 已修复或基本修复

1. 支付密钥不再硬编码在 `backend/app.py` 中，已改为从环境变量读取。
2. `backend/app.py` 的 CORS 已改为环境变量白名单，且 `allow_credentials=False`。
3. 后端固定金额已从 `1.00` 调整为默认 `49.00`，与前端 `¥49 / 月` 基本对齐。
4. 脚本生成器与分镜工作台已统一提交 `{ tool_id, payload }`。
5. 首页顶部 CTA 已改为 `Link`，可以跳转到 `/login` 和 `/dashboard`。
6. 登录、支付、用户信息等页面已集中使用 `frontend/lib/app-config.ts` 管理 token key 与 API URL。
7. 支付成功后写入的 token 已统一走 `storeSession()`，不再写入旧的 `x_token`。
8. `html lang` 已从 `en` 改为 `zh-CN`。

### 当前阻断

1. 前端 `npm run build` 失败，无法生成生产包。
2. 后端 `requirements.txt` 缺少 `cryptography`，新环境部署可能直接启动失败。
3. `backend/main.py` 仍保留旧版宽松 CORS 与旧后端入口，如果部署入口误配为 `main:app` 会绕开新版修复。
4. AI 工具接口结构已打通，但当前环境未配置 `DEEPSEEK_API_KEY`，实际生成能力仍不可用。

## 二、验证结果

| 检查项 | 结果 | 说明 |
|---|---|---|
| `npm run build` | 失败 | Next 编译成功，但类型检查失败：`Spotlight` 不支持 `fill` prop |
| `python -m py_compile app.py main.py` | 通过 | 后端两个入口文件语法检查通过 |
| `python -c "import app"` | 通过 | 当前本机环境可导入 `backend/app.py` |
| `POST /api/v1/tools` 本地探针 | 结构通过，业务失败 | 返回 200，失败原因是 `DEEPSEEK_API_KEY 未配置` |
| 首页顶部 CTA | 已修复 | `Navbar` 中 `/login`、`/dashboard` 已使用 `Link` |
| token key | 已修复 | 统一为 `xcc_token` |
| 支付金额 | 基本修复 | 前端展示与后端默认金额均为 49.00 |
| 首页占位链接 | 未完全修复 | `app/page.tsx` 仍有 8 处 `href="#"` |

## 三、优先级问题

### P0 上线阻断

#### 1. 前端生产构建失败

- 位置：`frontend/app/page.tsx:41`
- 关联位置：`frontend/components/ui/spotlight.tsx:6-10`
- 现象：`npm run build` 在类型检查阶段失败。
- 报错摘要：`Property 'fill' does not exist on type 'SpotlightProps'`
- 原因：页面传入 `<Spotlight ... fill="white" />`，但 `SpotlightProps` 只声明了 `className`、`size`、`springOptions`。
- 影响：当前版本无法正常完成生产构建，不能作为可发布版本交付。
- 建议：
  - 如果 `fill` 不再需要，移除 `app/page.tsx:41` 的 `fill="white"`。
  - 如果组件设计需要支持颜色，给 `SpotlightProps` 增加 `fill?: string` 并实际映射到样式。
  - 修复后重新运行 `npm run build`，继续暴露下一批类型问题。

### P1 功能与部署风险

#### 2. `requirements.txt` 缺少 `cryptography`

- 位置：`backend/requirements.txt:1-4`
- 关联位置：`backend/app.py:18-20`
- 现象：`app.py` 使用了 `cryptography` 的签名、验签与 AES-GCM 能力，但依赖文件只声明了 `fastapi`、`uvicorn[standard]`、`pydantic`、`requests`。
- 本机结果：当前机器可 `import app`，说明全局环境里已有该包。
- 风险：全新部署环境只按 `requirements.txt` 安装时，后端会因缺包启动失败。
- 建议：把 `cryptography` 加入 `backend/requirements.txt`，并在干净虚拟环境中验证后端启动。

#### 3. 旧后端入口 `backend/main.py` 仍保留不安全 CORS

- 位置：`backend/main.py:10-12`
- 现象：旧入口仍是 `allow_origins=["*"]` 与 `allow_credentials=True`。
- 对比：新版主入口 `backend/app.py:47-48` 已改为白名单来源且 `allow_credentials=False`。
- 风险：如果部署命令写成 `uvicorn main:app`，线上会运行旧入口，只暴露 `/api/v1/tools` 和 `/health`，并重新带回宽松 CORS。
- 建议：
  - 明确部署入口为 `uvicorn app:app`。
  - 删除或收敛 `main.py`，避免两个 FastAPI app 长期并存。
  - 如果必须保留 `main.py`，至少同步 CORS、接口与配置策略。

#### 4. AI 生成能力依赖 `DEEPSEEK_API_KEY`，当前环境未配置

- 位置：
  - `backend/tools/script_generator.py:9`
  - `backend/tools/video_storyboard.py:9`
- 探针结果：
  - `POST /api/v1/tools` + `script_generator` 返回 200，但 `success=False`
  - `POST /api/v1/tools` + `video_storyboard` 返回 200，但 `success=False`
  - 失败信息均为 `DEEPSEEK_API_KEY 未配置`
- 影响：前后端协议已经对齐，但用户仍无法得到真实脚本/分镜结果。
- 建议：
  - 在后端运行环境配置 `DEEPSEEK_API_KEY`。
  - 前端对 `success=False` 的错误提示已经能展示，但上线前仍需用真实 key 做一次端到端生成验证。

#### 5. 微信登录/支付仍依赖完整环境变量，当前未做真实链路复测

- 位置：
  - `backend/app.py:55-66`
  - `backend/app.py:78-105`
  - `backend/app.py:584-636`
- 现状：支付、登录配置已迁移到环境变量，并增加了缺失/占位值校验。
- 未验证原因：当前本地环境没有提供真实微信支付、公众号、回调公网地址等配置。
- 风险：代码结构问题已减少，但真实扫码登录、Native 支付、微信回调验签仍需要生产级配置才能闭环验证。
- 建议：配置真实环境变量后，按“扫码登录 -> 创建订单 -> 微信支付 -> webhook 回调 -> token 更新 -> 进入 `/generate`”完整复测。

### P2 体验与维护问题

#### 6. 首页仍有多处占位链接

- 位置：`frontend/app/page.tsx:162,171,180,189,198,477,483,489`
- 现象：多个功能卡片 CTA 和社媒图标仍指向 `href="#"`。
- 影响：用户点击后没有有效去向，转化体验和可信度受影响。
- 建议：
  - 功能卡片优先指向 `/dashboard`、`/tools/script-generator` 或 `/generate`。
  - 社媒链接如果暂无真实账号，建议暂时移除或改成有效的联系方式。

#### 7. 旧版 `video-tool` 页面未统一使用 API helper

- 位置：`frontend/app/video-tool/page.tsx:63`
- 现象：该页面直接请求 `fetch("/api/v1/tools")`，没有使用 `buildApiUrl()`。
- 影响：在 Next rewrites 生效时可用，但如果前端以静态资源方式部署，或需要显式走 `NEXT_PUBLIC_API_BASE`，该旧页会和新版页面行为不一致。
- 建议：如该页面仍保留入口，应统一改为 `fetch(buildApiUrl("/api/v1/tools"))`；如已废弃，建议下线。

#### 8. API Base 策略已改善，但仍需部署约定落地

- 位置：
  - `frontend/lib/app-config.ts:4-7`
  - `frontend/next.config.mjs:1-10`
- 现状：
  - 页面侧大多通过 `buildApiUrl()` 使用 `NEXT_PUBLIC_API_BASE`。
  - Next rewrite 默认代理到 `http://127.0.0.1:8000`，也可通过 `BACKEND_PROXY_TARGET` 覆盖。
- 风险：如果部署方式是纯静态托管，Next rewrite 不会生效；如果是 Next 服务端部署，则需要确保 `BACKEND_PROXY_TARGET` 指向真实后端。
- 建议：确定唯一部署模式，并把 `.env.example`、启动命令和部署说明补齐。

## 四、上一版问题状态对照

| 上一版问题 | 当前状态 | 备注 |
|---|---|---|
| 支付密钥硬编码 | 已修复 | `app.py` 已改为环境变量读取；仍需确认已轮换真实泄露密钥 |
| 脚本生成器请求结构不一致 | 已修复 | 前端已发送 `{ tool_id, payload }` |
| 生成工作台请求结构不一致 | 已修复 | 前端已发送 `{ tool_id, payload }` |
| 首页顶部 CTA 死按钮 | 已修复 | `Navbar` 已使用 `Link` |
| 支付 token key 不一致 | 已修复 | 统一走 `storeSession()` / `xcc_token` |
| 前后端价格不一致 | 基本修复 | 默认均为 49.00；建议继续让 plan 与金额由后端/配置统一下发 |
| API 基地址混乱 | 基本修复 | 主要页面已统一；旧 `video-tool` 页面仍需处理 |
| CORS 不安全 | 主要入口已修复 | `app.py` 已修复，`main.py` 仍残留 |
| `html lang="en"` | 已修复 | 已改为 `zh-CN` |
| 首页占位链接 | 未完全修复 | 仍有 8 处 `href="#"` |

## 五、建议修复顺序

### 第一批：立即处理

1. 修复 `Spotlight` 的 `fill` 类型错误，恢复 `npm run build`。
2. 将 `cryptography` 加入 `backend/requirements.txt`。
3. 明确后端部署入口为 `app:app`，同步或移除 `main.py`。
4. 配置 `DEEPSEEK_API_KEY`，复测脚本生成器与分镜工作台。

### 第二批：上线前处理

1. 配置并复测微信登录、支付、回调、token 更新全链路。
2. 清理首页全部 `href="#"` 占位链接。
3. 统一或下线 `video-tool` 旧页面。
4. 补充 `.env.example` 与部署说明，明确 `NEXT_PUBLIC_API_BASE`、`BACKEND_PROXY_TARGET`、微信配置、DeepSeek 配置。

## 六、验证命令记录

```bash
cd frontend
npm run build
```

结果：失败，阻断点为 `frontend/app/page.tsx:41` 的 `Spotlight fill` 类型错误。

```bash
cd backend
python -m py_compile app.py main.py
python -c "import app; print(app.app.title)"
```

结果：语法检查与导入检查通过。

```python
from fastapi.testclient import TestClient
import app

client = TestClient(app.app)
client.post("/api/v1/tools", json={"tool_id": "script_generator", "payload": {"video_url": "https://example.com", "keywords": "测试"}})
client.post("/api/v1/tools", json={"tool_id": "video_storyboard", "payload": {"characterProfile": "角色", "storyText": "故事"}})
```

结果：接口结构可用，但因 `DEEPSEEK_API_KEY 未配置` 返回业务失败。
